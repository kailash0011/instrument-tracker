PORT=5000
# IMPORTANT: Replace this with a strong random secret before deploying to production
# Generate one with: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=instrument_tracker_secret_2024
DB_PATH=./data/instrument_tracker.db
